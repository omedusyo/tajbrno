- @title = @image.alt

.twele.columns
  %h1 Displaying a particular gallery imgage...

- if has_access_to? :gallery
  .twelve.columns
    .medium.info.btn
      = link_to 'Edit', action: :edit, id: @image.id
    .medium.danger.btn
      = link_to 'Remove', delete_gallery_image_url(@image.id), method: :delete

= show_notice

.twelve.columns
  .image= image_tag @image.image_url.to_s
