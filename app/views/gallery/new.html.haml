- @title = 'Add new image'

.twelve.columns
  %h1 Add new gallery image

.twelve.columns
  = form_for @image, url: { action: :create }, html: { multipart: true } do |f|
    = render partial: 'form', locals: { f: f }

    .large.info.btn= submit_tag 'Add new image'
