- @title = 'New category'

.twelve.columns
  %h1 Create new category

.twelve.columns
  = form_for @category, url: { action: :create } do |f|
    = render partial: 'form', locals: { f: f }

    .large.info.btn= submit_tag 'Create'
