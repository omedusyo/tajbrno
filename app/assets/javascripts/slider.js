(function ($) {

  $.fn.slider = function (options) {

    var defaults = {

    };

    var settings = $.extend({}, defaults, options);
    var $this = $(this);

    // TODO: body of the slider

    return this;
  };
})(jQuery);

